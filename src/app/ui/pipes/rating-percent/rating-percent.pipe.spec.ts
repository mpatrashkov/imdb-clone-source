import { RatingPercentPipe } from './rating-percent.pipe';

describe('RatingPercentPipe', () => {
  it('create an instance', () => {
    const pipe = new RatingPercentPipe();
    expect(pipe).toBeTruthy();
  });
});
